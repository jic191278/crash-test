import React from 'react';

const Form = () => {
  return (
    <div className="form">
      <form>
        <input type="email" />
        <input type="password" />
        <button type="submit">Submit</button>
      </form>
    </div>
  );
};

export default Form;
